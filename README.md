# payx-backend

Node.js backend for the PayX USDT gaming wallet system.

## Render Deployment

Use these settings:

```text
Root Directory: backend
Runtime: Node
Build Command: npm install
Start Command: npm start
```

Do not commit `.env`. Add the Render environment variables from `backend/.env.example`.
